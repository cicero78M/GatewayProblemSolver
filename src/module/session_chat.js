export async function createSession(chat) {

    console.log(chat);

    //Create New Chat Session
    //ChatID
    //Contacts
    //Name
    //State
    //timestamp
    //readState
    //seenState
    //responder

    //While the new contact create messages then this function create session data, 

    // try {
    //     writeFileSync(`json_data_file/insta_data/insta_likes/${clientName}/${todayItems[i]}.json`, JSON.stringify(encryptedData));
    //   } catch (error) {
    //     mkdirSync(`json_data_file/insta_data/insta_likes/${clientName}`);
    //     writeFileSync(`json_data_file/insta_data/insta_likes/${clientName}/${todayItems[i]}.json`, JSON.stringify(encryptedData));
    //   } 
    
}